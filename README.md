# SocialPulse - 社交媒体舆情分析系统

<div align="center">

![Python](https://img.shields.io/badge/Python-3.11+-blue.svg)
![License](https://img.shields.io/badge/License-MIT-green.svg)
![Status](https://img.shields.io/badge/Status-Active-success.svg)

**一个强大的社交媒体情报分析平台，用于识别协同造假网络与异常账号**

[功能特性](#功能特性) • [快速开始](#快速开始) • [系统架构](#系统架构) • [使用文档](#使用文档)

</div>

---

## 项目简介

SocialPulse 是一个完整的社交媒体舆情分析系统，能够从 Twitter/X、微博、Reddit、Telegram 等主流平台采集数据，通过多维特征工程和图神经网络技术识别水军、机器人账号和协同造假网络。

### 核心能力

- 🔍 **多平台数据采集** - 支持 Twitter、微博、Reddit、Telegram 四大平台
- 🛡️ **反爬对抗** - 代理池、限流器、指纹混淆、验证码处理
- 📊 **五维特征工程** - 时序、内容、网络、元数据、跨平台特征
- 🧠 **图神经网络** - RGCN + GAT 异构图神经网络模型
- 🔗 **跨事件关联** - 识别账号资产库与复用模式
- 📈 **可视化平台** - Streamlit 交互式仪表盘
- 📝 **证据链生成** - SHAP 特征归因与自动化报告

---

## 功能特性

### 数据采集层

| 平台 | 采集方式 | 功能 |
|------|---------|------|
| Twitter/X | API v2 + 网页爬取 | 关键词搜索、用户时间线、话题标签、流式监听 |
| 微博 | 移动端 API + 网页 | 搜索采集、用户主页、超话内容、热搜榜单 |
| Reddit | 官方 API | Subreddit 监控、评论树采集、用户帖子 |
| Telegram | MTProto 协议 | 频道消息、群组消息、消息搜索 |

### 特征工程

```
五维特征空间 F = {F_t, F_c, F_s, F_n, F_m}

F_t: 时序行为特征 - 发布频率、时间熵、自相关系数、响应延迟
F_c: 内容生成特征 - 文本相似度、话题一致性、情感分析、模板检测
F_s: 社交网络特征 - 中心性指标、聚类系数、社区归属
F_n: 账号元数据特征 - 注册聚集性、用户名模式、头像哈希
F_m: 跨平台关联特征 - 身份匹配、行为指纹、跨事件关联
```

### 异常检测算法

- **孤立森林** (Isolation Forest) - 高维特征异常检测
- **局部异常因子** (LOF) - 局部密度异常检测
- **Z-Score** - 统计异常检测
- **自编码器** (AutoEncoder) - 重构误差异常检测
- **集成检测器** - 多算法融合

### 图分析能力

- **社区发现**: Louvain 算法、标签传播算法 (LPA)
- **中心性分析**: PageRank、介数中心性、特征向量中心性
- **传播分析**: 传播路径追踪、影响力节点识别
- **图神经网络**: RGCN 关系图卷积、GAT 图注意力网络

---

## 快速开始

### 环境要求

- Python 3.11+
- SQLite / PostgreSQL (可选)
- Redis (可选)

### 安装步骤

```bash
# 克隆仓库
git clone https://github.com/yourusername/social-pulse.git
cd social-pulse

# 创建虚拟环境
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
# .venv\Scripts\activate  # Windows

# 安装依赖
pip install -r requirements.txt

# 配置环境变量
cp .env.example .env
# 编辑 .env 文件，填入 API 密钥等配置

# 初始化数据库
python -m sentiment_analyzer.main init
```

### 启动服务

```bash
# 启动可视化平台
python -m streamlit run sentiment_analyzer/dashboard/app.py

# 或使用 Docker
docker-compose up -d
```

访问 http://localhost:8501 查看仪表盘。

---

## 系统架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        数据源层 (Data Sources)                    │
│   Twitter/X API  │  微博API  │  Reddit API  │  Telegram API     │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                       数据采集层 (Ingestion)                      │
│   分布式爬虫集群  │  API流式接入  │  反爬对抗模块  │  数据清洗      │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                      数据存储层 (Storage)                         │
│   关系数据库  │  时序数据库  │  图数据库  │  搜索引擎  │  消息队列  │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                      分析引擎层 (Analytics)                       │
│   特征工程引擎  │  图计算引擎  │  机器学习平台  │  文本挖掘引擎    │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                      应用服务层 (Application)                     │
│   可视化平台  │  证据生成系统  │  报告自动化  │  预警推送服务     │
└─────────────────────────────────────────────────────────────────┘
```

---

## 项目结构

```
sentiment_analyzer/
├── crawlers/                    # 数据采集层
│   ├── base.py                  # 爬虫基类
│   ├── rate_limiter.py          # 令牌桶限流器
│   ├── proxy_pool.py            # 代理池管理
│   ├── bloom_filter.py          # 布隆过滤器
│   ├── cleaner.py               # 数据清洗管道
│   ├── twitter.py               # Twitter/X适配器
│   ├── weibo.py                 # 微博适配器
│   ├── reddit.py                # Reddit适配器
│   ├── telegram.py              # Telegram适配器
│   └── anti_detect.py           # 反爬对抗模块
├── storage/                     # 数据存储层
│   ├── models.py                # Pydantic数据模型
│   ├── database.py              # 数据库管理
│   ├── repository.py            # 数据访问层
│   ├── graph_store.py           # 图数据库存储
│   ├── search.py                # 搜索引擎
│   └── queue.py                 # 消息队列
├── analysis/                    # 分析引擎层
│   ├── features.py              # 特征工程
│   ├── anomaly.py               # 异常检测算法
│   ├── graph.py                 # 图分析模块
│   ├── gnn.py                   # 图神经网络模型
│   └── cross_event.py           # 跨事件关联分析
├── dashboard/                   # 可视化层
│   ├── app.py                   # Streamlit仪表盘
│   ├── evidence.py              # 证据链生成
│   └── report.py                # 报告自动化
├── monitoring/                  # 监控模块
│   └── metrics.py               # 指标采集与告警
├── tests/                       # 测试模块
├── config/                      # 配置模块
└── main.py                      # CLI入口
```

---

## 使用文档

### CLI 命令

```bash
# 初始化数据库
python -m sentiment_analyzer.main init

# 数据采集
python -m sentiment_analyzer.main crawl -p twitter -q "AI" -l 100

# 执行分析
python -m sentiment_analyzer.main analyze -p twitter -l 1000

# 启动仪表盘
python -m sentiment_analyzer.main dashboard

# 查看统计
python -m sentiment_analyzer.main stats
```

### Python API

```python
from sentiment_analyzer.crawlers import TwitterCrawler, TwitterConfig
from sentiment_analyzer.analysis import FeatureExtractor, IsolationForestDetector

# 数据采集
config = TwitterConfig(bearer_token="your_token")
async with TwitterCrawler(config) as crawler:
    async for post in crawler.crawl_keywords(["AI"], limit=100):
        print(post.content)

# 特征提取
extractor = FeatureExtractor()
features = extractor.extract_post_frequency(posts)

# 异常检测
detector = IsolationForestDetector(contamination=0.1)
detector.fit(feature_matrix)
predictions = detector.predict(test_features)
```

---

## Docker 部署

```bash
# 构建并启动
docker-compose up -d

# 查看日志
docker-compose logs -f app

# 停止服务
docker-compose down
```

---

## 技术栈

| 层级 | 技术组件 |
|------|---------|
| 数据采集 | Playwright + asyncio + aiohttp |
| 数据存储 | SQLite + NetworkX + Whoosh |
| 机器学习 | scikit-learn + PyTorch + PyG |
| 图分析 | NetworkX + Neo4j (可选) |
| 可视化 | Streamlit + Pyecharts + Plotly |
| 部署 | Docker + Docker Compose |

---

## 应用场景

- 🎯 **舆情监测** - 追踪话题热度与情感趋势
- 🤖 **水军识别** - 检测机器人账号与协同造假网络
- 📊 **影响力分析** - 识别关键传播节点与意见领袖
- 🔗 **跨事件关联** - 追踪账号资产库与复用模式
- 📝 **证据生成** - 自动化生成可解释的分析报告

---

## 许可证

本项目采用 MIT 许可证。详见 [LICENSE](LICENSE) 文件。

---

## 致谢

本项目参考了以下技术与研究成果：

- [Twitter API v2 Documentation](https://developer.twitter.com/en/docs/twitter-api)
- [PyTorch Geometric](https://pytorch-geometric.readthedocs.io/)
- [NetworkX](https://networkx.org/)
- [Streamlit](https://streamlit.io/)

---

<div align="center">

**⭐ 如果这个项目对你有帮助，请给一个 Star！**

</div>
